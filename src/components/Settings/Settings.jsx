import React from 'react';
import s from './Settings.module.css';

const Settings = props => {
  return (
    <div className={s.settings}>
      <h3 className={s.page_settings}>Page Settings</h3>
    </div>
  );
};
export default Settings;
